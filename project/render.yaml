services:
  - type: web
    name: dragon-duel-arena
    runtime: static
    buildCommand: echo "Static dragon arena ready"
    staticPublishPath: .
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: true
